<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The link to the head unit: a TCP server on the phone's hotspot address. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Tells the hotspot apart from the phone's own networks, to listen on it alone. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Kept listening while the screen is off. connectedDevice needs one of a
         short list of permissions besides; CHANGE_NETWORK_STATE is the plain one. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Asks once to be left out of battery optimisation, so the link survives
         long drives with the phone in a pocket. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Calls on the car's screen: who is calling (state, number from the call
         log permission, name and photo from the contacts), answer and hang up. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- The pairing secret lives in this app's preferences: never in a backup,
         nor in a device-to-device transfer (which allowBackup alone doesn't stop on Android 12+). -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Companion">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- The launcher's pairing QR code, opened from any QR scanner (the
                 camera and Lens open links as BROWSABLE). Nothing is paired until
                 the confirm dialog, naming the car, is accepted; and the phone only
                 listens on its own hotspot, so a planted pairing has no reach. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dashwheel" android:host="pair" />
                <!-- A second-screen display's code, read with the phone's own camera app. -->
                <data android:scheme="dashwheel" android:host="display" />
            </intent-filter>
        </activity>

        <!-- The QR scanner follows the phone's orientation instead of forcing landscape. -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />

        <service
            android:name=".LinkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <service
            android:name=".PhoneNotificationListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
